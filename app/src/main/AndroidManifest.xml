<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aop.part2.mask">
<!--    xmlns:tools="http://schemas.android.com/tools">-->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mask"
        android:networkSecurityConfig="@xml/network_security_config"
        >

        <!--        android:fullBackupContent="@xml/backup_rules"-->
        <!--        android:dataExtractionRules="@xml/data_extraction_rules"-->
        <!--     tools:targetApi="30"   >-->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

<!--            <meta-data-->
<!--                android:name="android.app.lib_name"-->
<!--                android:value="" />-->
        </activity>
        <activity android:name=".presentation.login.LoginActivity"/>
        <activity android:name=".presentation.mypage.MypageActivity"/>
        <activity android:name=".presentation.library.MylibraryActivity"/>
        <activity android:name=".presentation.test.TestActivity"/>

<!--        <activity android:name=".RecordButton"/>-->
<!--        <activity android:name=".SoundVisualizerView"/>-->
<!--        <activity android:name=".State"/>-->

    </application>

</manifest>